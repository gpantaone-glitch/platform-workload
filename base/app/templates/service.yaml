apiVersion: v1
kind: Service
metadata:
  name: platform-app
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: platform-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: {{ .Values.service.type }}
