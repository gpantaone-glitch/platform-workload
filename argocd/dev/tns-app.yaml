apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tns-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/gpantaone-glitch/platform-workload
    targetRevision: main
    path: base/tns-app
    helm:
      valueFiles:
        - ../../../apps/dev/tns-app/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: tns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
